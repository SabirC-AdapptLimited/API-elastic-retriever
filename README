Use the .env.example to create a .env file, and update the relevant details (including the endpoint).

```
npm run dev
```

Route runs @ /getContent and using a GET request with a deparments query param. i.e., `localhost:3000/getContent?departments=someTagName`
